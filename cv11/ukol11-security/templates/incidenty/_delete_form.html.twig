<form method="post" action="{{ path('app_incidenty_delete', {'id': incidenty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ incidenty.id) }}">
    <button class="btn">Delete</button>
</form>
